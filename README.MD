# Hola Este videojuego

git config --global user.name ""
git config --global user.email ""

git clone https://github.com/fatimamtz/PROYECTO-SURVIVAL.git .

git checkout -b ""

# merge, push, checkout
unir
cambiar
crear

subir 